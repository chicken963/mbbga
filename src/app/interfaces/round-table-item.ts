export interface RoundTableItem {
    audioFileId: string,
    artist: string,
    title: string,
    versionId: string,
    startTime: number,
    endTime: number,
    duration: number,
    mode: string
}